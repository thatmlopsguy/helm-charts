deployments:
  web:
    enabled: true
    image:
      repository: nginx
      tag: latest
    ports:
      - name: http
        containerPort: 80
    autoscaling:
      enabled: true
      minReplicas: 2
      maxReplicas: 5
      targetCPUUtilizationPercentage: 80
